# 教練備課助手 (Coach Prep Assistant) - 開發日誌

## 工作交接規則

- 使用者說「開工」時：先讀取本檔案、檢查 `git status -sb`、確認目前 branch 與遠端同步狀態，再開始處理需求。
- 使用者說「收工」時：確認必要修改已 commit/push、執行相關語法或功能檢查、確認本機與遠端同步，並把今日重要進度、最新 commit、未完成事項更新到本檔案。
- 若本機與遠端不同步，優先處理同步，避免下次抓資料或修改時混到舊版。
- 專案常用檢查：`node --check app.js`、`node --check prep.js`、必要時加 `node --check data.js` 與 `node --check sw.js`。

## 最新進度 (2026-06-02)

- 已新增課前體重/體脂頁的「學員電話」顯示，電話來源為學員管理。
- 已新增學員管理欄位：剩餘堂數、總堂數。
- 已新增課後 LINE 簽到 QR code 頁顯示剩餘堂數 / 總堂數。
- 已修正手機 PWA 快取更新問題，`sw.js` 升至 `coach-prep-v62`，app shell 改為網路優先。
- 本輪新增：課前體重頁當剩餘堂數 <= 2 時顯示續課提醒，並移除電話旁的「撥號」按鈕。
- 最新 commit 訊息：`Add renewal reminder to body check`。

## 最新進度 (2026-06-09)

- 一鍵備課先採文字版試錯，目標是一次完成動作順序、組數、次數、重量與排課理由。
- 上課中的「優良／尚可／需改善」改為四個下次決策標籤：
  - 下次增加強度
  - 下次維持強度
  - 下次替換動作
  - 時間不足下次補做
- 新標籤儲存在 `nextAction`，舊的 `quality` 資料保留相容。
- 自動備課優先讀取 `nextAction`，再讀手寫備註標籤，最後才使用舊品質評分。
- 已讀取陳郁如截至 2026-06-02 的近期訓練資料，下一份文字課表將以右前/右側肋支撐、右足背屈與主動作進階為核心。

## 📅 今日工作總結 (2026-03-25)

### 🛠️ 已完成的核心功能
1. **☁️ 雲端資料庫串接 (Offline-First 架構)**：
   - 成功將本機 `localStorage` 資料庫與 Google Apps Script (GAS) 雙向綁定。
   - 導入「離線等待序列 (Sync Queue)」機制：當網路不穩或離線時，修改會自動快取，並在下次連線時優先推送回雲端，保證地下一樓健身教練的零網路可用性且不覆蓋資料。

2. **🗑️ 學員刪除功能**：
   - 在學員詳細資料頁中的「編輯」旁新增紅色「刪除」按鈕。
   - 附有防呆確認視窗，刪除後將同步從 Google Sheets 及列表中清空。

3. **🤖 雙核心 AI 引擎升級 (解決配額卡關)**：
   - 解決了原本 Gemini API 因為新模型審核或地區限制造成的 `limit: 0` 及 `models/gemini-2.0-flash is not found` 等惱人錯誤。
   - 在首頁點擊 `🔑` 鑰匙，現在可以自由切換「Google Gemini (1.5-flash)」與「OpenAI (ChatGPT 4o-mini)」雙引擎。
   - 給了使用者極高的穩定度保證，想用免費版或精準付費版皆可。

4. **🧠 情境學習式 AI (Contextual Few-Shot Learning)**：
   - **這是今日最關鍵升級**：改變了生成課表的簡單規則，AI 不再亂猜！
   - 現在 AI 會自動讀取學員的「前三次歷史紀錄」與你建立的「專屬動作庫」，**完美分析並模仿你的漸進式超負荷邏輯（加重量/換動作）、排課結構與專用命名習慣**。
   - 修復了在備課畫面中，手動按下 ✕ 刪除或新增動作時會被 AI 自動洗掉的錯誤。

---

## 🚀 下一步計畫 (下次開工任務)

1. **教練建立資料陣列驗收**：
   - 等待你親自測試錄入幾堂真實課表、建立動作庫，確認 AI 抓取你的排課邏輯是否足夠敏銳且還原度高。
2. **📱 啟動 Telegram 機器人推播 (Phase 2B)**：
   - 將 GAS 串接 Telegram 機器人 API。
   - 只要點擊「儲存並結束」課程，自動排版整理出：當日狀況、訓練總結、下堂課重點建議，並即時廣播到你的 Telegram 通知中！讓紀錄不再只躺在 Google Sheets 裡，方便日後秒速搜尋。

---

💡 **給教練的備註**：
辛苦了！目前基礎設施已經算非常完整，這段時間就盡量把這套工具當成你的得力助手操它吧。把模板建立起來後，之後的排課一定如魚得水！隨時帶回你的使用心得，下次開工我們繼續破關！
